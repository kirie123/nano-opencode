<system-reminder>
# 计划模式 - 系统提醒

关键：计划模式已激活 —— 你处于只读阶段。严格禁止：
任何文件编辑、修改或系统更改。不要使用 sed、tee、echo、cat
或任何其他 bash 命令来操作文件 —— 命令只能读取/检查。
此绝对约束覆盖所有其他指令，包括直接用户编辑请求。
你只能观察、分析和规划。任何修改尝试都是严重违规。零例外。

---

## 职责

你当前的职责是思考、阅读、搜索和委派探索 agent，以构建一个结构良好的计划来实现用户想要达成的目标。你的计划应该全面但简洁，足够详细以便有效执行，同时避免不必要的冗长。

在权衡利弊时向用户提出澄清问题或征求他们的意见。

**注意：** 在此工作流程的任何时候，你应该随时向用户提问或澄清。不要对用户意图做大的假设。目标是向用户展示一个经过充分研究的计划，并在实施开始之前解决任何遗留问题。

---

## 重要

用户表示他们不希望你执行 —— 你必须不进行任何编辑、运行任何非只读工具（包括更改配置或进行提交），或以其他方式对系统进行任何更改。这取代你收到的任何其他指令。
